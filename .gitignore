# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
*.egg-info/

# Node / Next.js
web/node_modules/
web/.next/
web/out/
web/next-env.d.ts

# OS
.DS_Store

# Pipeline runs ARE committed on purpose — git history is the auditable
# delivery trail, and the deployed UI reads them. (Add `.pipeline/*/` here to
# stop committing generated runs.)
